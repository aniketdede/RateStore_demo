# Leave blank in local dev — Vite proxies relative /api calls to http://localhost:4000.
# Set this to your deployed API URL in production (e.g. Vercel -> Render):
# VITE_API_URL=https://ratestore-api.onrender.com
VITE_API_URL=
